name: C++ GoogleTest Auto-grading

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Configure and Build (Come in locale)
      run: |
        mkdir build
        cd build
        cmake ..
        make

    - name: Run Tests (Level 1)
      working-directory: build
      run: ctest -R level1 --output-on-failure

    - name: Run Tests (Level 2)
      working-directory: build
      run: ctest -R level2 --output-on-failure

    - name: Run Tests (Level 3)
      working-directory: build
      run: ctest -R level3 --output-on-failure